# README brand and remote connections

User requested the current logo, launch background and remote connection promotion. Both root language variants now reference the approved application PNGs directly, retaining original aspect ratios and avoiding duplicate or generated artwork. The background is labeled as artwork, not a screenshot. Feature summaries remain near the top.

Remote documentation describes the existing SSH path, computer editing, explicit test/connect state, fixed tabs, actual DSH port discovery and SSH authentication prerequisites. Web credential exchange is not described as private-key transfer. The independent pairing server and client extension are explicitly marked as unfinished desktop P2P integration; no release-completion claim is added.

Validation: inspected both source images, cross-checked remote UI/authentication implementation, checked English/Chinese local links and asset references, and ran Prettier plus git diff whitespace checks. Documentation only; no runtime or release behavior changed.
