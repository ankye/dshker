## 1. Cross-repository protocol

Scope note: this change requires no DeepSeek Harness modification. Harness ships
no `desktop` application profile, descriptor parser, or child IPC counterpart, so
the launcher starts the standard `dsh web --no-open` command and takes readiness
from the URL that process announces. No private-bridge scaffolding ships with this
change; implementing that transport belongs to a separate change owning both
repositories.

- [x] 1.1 [Owner: Integration] Confirm no Harness-side protocol work is required and record the launch contract actually used. Dependencies: none. Evidence: `design.md` records the `dsh web --no-open` decision and the rejected descriptor/IPC alternative; `$DSH_HOME/profiles` has no usable `desktop` profile, `apps/cli/src` exposes no descriptor flag, and `packages/bundle/` ships `web-app` with no `desktop-app`. The undeclared `desktop-harness-client-bridge` capability was retired: its runtime requirements moved into `harness-runtime-supervision`, its renderer limits became the declared `desktop-renderer-authority`, and 1848 lines of unwired descriptor/IPC scaffolding were deleted.
- [x] 1.2 [Owner: Integration] Freeze the readiness contract as the child's own announced loopback URL. Dependencies: 1.1. Evidence: `electron/main/managed/launcher-harness-service.test.ts` accepts the real `dsh web: <url>` line with its credential and rejects non-loopback hosts, non-http schemes, prose mentions, and malformed announcements.
- [x] 1.3 [Owner: Integration] Record the exact compatible Harness commit and its verification evidence in the Launcher compatibility record. Dependencies: 1.1, 1.2. Evidence: DSHKer on 2026-09-03 launched managed Harness commit `db6bdc3576c2d4e7c965e8e3ed0c2a731eed87f5`, accepted the child-announced loopback URL, and stopped the exact managed process. The native web profile contained only the in-box base and web bundles during this smoke.

## 2. Launcher foundation and four-root registry

- [x] 2.1 [Owner: Launcher] Establish typed Electron main, preload, renderer, IPC, native-dialog, and locale boundaries for the desktop product. Dependencies: none. Evidence: `npm run type-check` and `npm run architecture:check` pass; 22 named handlers in `electron/main/ipc.ts` with contracts in `src/shared/contracts.ts`; `npm test -- --run` covers i18n and routing.
- [x] 2.2 [Owner: Launcher] Implement the bootstrap locator and strict registry for platform-default Launcher storage, plus explicit Settings relocation. Dependencies: 2.1. Evidence: `electron/main/managed/registry.test.ts` and `installation-catalog.test.ts` reject malformed and unsupported records.
- [x] 2.3 [Owner: Launcher] Validate canonical containment, non-overlap, fresh-root ownership, symbolic-link/junction escape, and native Harness-home exclusion. Dependencies: 2.2. Evidence: `electron/main/managed/git/paths.test.ts` and `service.test.ts` cover valid and invalid root layouts.
- [ ] 2.4 [Owner: Launcher] Create validated source and internal namespaces while using the DSH-owned profile, plugin directory, and Agent-preset directory without direct Launcher writes. Dependencies: 2.3. The Launcher copies native-selected local sources or clones HTTPS Git sources under `~/.dshlauncher/plugins`, installs only those copies through `dsh plugin --profile web`, and removes the exact recorded source only after DSH uninstalls it. The Extensions list reads only the native profile, groups companion packages by source, and never presents cached source directories as another installation list. Git records persist the clone URL, branch, package path, and checked-out commit, which the Extensions list projects and updates through `dsh plugin --profile web update <package>`. An explicit first update may move a local GitHub checkout under management by deriving its selected branch and package child; it never mutates that external checkout. Evidence: `managed-plugin-sources.test.ts` proves a local copy, package-to-source mapping, exact cleanup, escape-record refusal, and Git revision projection; `launcher-harness-service.test.ts` covers Git catalog tree parsing and local-checkout tree address derivation; `versionViewState.test.ts` covers grouped runtime/settings extensions and update states. A 2026-09-03 live DSHKer run installed, updated, and uninstalled `@deepseek-ai/dsh-tool-vision` through its Git source, then recovered the default core after that plugin's own compatibility failure. Remaining: no automated test proves a changed compatible plugin layer becomes visible to a Launcher-started run.
- [x] 2.5 [Owner: Launcher] Implement atomic registry/catalog writes and explicit recovery for interrupted Launcher-owned persistence. Dependencies: 2.2. Evidence: `electron/main/managed/registry.test.ts` and `installation-catalog.test.ts` cover interrupted-write recovery to one authoritative record.

## 3. Managed source and toolchain

- [x] 3.1 [Owner: Launcher] Register and revalidate explicit Git, Node.js, and pnpm executable identities with direct, shell-free probes. Dependencies: 2.1. Evidence: `electron/main/managed/toolchain/process.test.ts`, `manifest.test.ts`, and `semver.test.ts` cover changed-target, timeout, missing, and version-mismatch outcomes.
- [x] 3.2 [Owner: Launcher] Validate explicit Git remote identities and create managed mirrors only under the Harness root. Dependencies: 2.3, 3.1. Evidence: `electron/main/managed/git/remote.test.ts` (12 cases) and `mirror.test.ts` prove rejection of unsupported remotes and zero out-of-root writes.
- [x] 3.3 [Owner: Launcher] Resolve selected branch, tag, or commit references to exact SHAs and detect rewritten observations. Dependencies: 3.2. Evidence: `electron/main/managed/git/worktree.test.ts` resolves a fetched branch against real Git fixtures; `revision.ts` rejects ambiguous refs.
- [x] 3.4 [Owner: Launcher] Materialize, verify, retain, and explicitly select detached exact-SHA worktrees. Dependencies: 3.3. Evidence: `electron/main/managed/git/worktree.test.ts` covers clean detached materialization, `git.repository_dirty`, and containment refusal for an escaping installation id.
- [ ] 3.5 [Owner: Launcher] Prepare exact revision-bound plugin and configuration generations using the selected Node/pnpm tools and declared lockfile. Dependencies: 2.4, 3.1, 3.4. Partially delivered: `launcher-harness-service.ts` runs `git clean -xdf`, `pnpm install --frozen-lockfile`, and `pnpm run build` on version switch; `launcher-harness-service.test.ts` proves ignored and untracked residue is removed while tracked source remains. Remaining: generation identity is not bound to the SHA, and no test proves invalid tool or lockfile state blocks activation.

## 4. Runtime supervision and launch

- [x] 4.1 [Owner: Launcher] Prove the selected runtime inputs before launch instead of producing a private descriptor. Dependencies: 1.2, 3.4. Evidence: `launcher-harness-service.ts` `#readiness` requires a direct Harness directory, `package.json`, built `apps/cli/lib/bin.js`, and `.git` before any spawn; failure yields a typed `runtime.worktree_invalid`.
- [x] 4.2 [Owner: Launcher] Spawn only the registered executable and selected worktree CLI with the standard `web --no-open` arguments. Dependencies: 4.1. Evidence: `launcher-harness-service.ts` spawns with `shell: false`, the selected worktree as cwd, and an unmodified inherited environment, so `DSH_HOME` is neither set nor cleared.
- [x] 4.3 [Owner: Launcher] Derive readiness from the child's own announced URL and reject any other address. Dependencies: 1.2, 4.2. Evidence: `launcher-harness-service.test.ts` (7 cases) pins loopback-only http(s) acceptance, credential preservation, LAN-suffix handling, and rejection of prose mentions.
- [x] 4.4 [Owner: Launcher] Implement runtime state, bounded diagnostics, stop, and mutation blocking. Dependencies: 4.3. Evidence: bounded 1000-entry stdout/stderr console; `stop()` accepts `starting` and `running` and rejects otherwise with `runtime.not_found`; `#assertReadyForVersionOperation` blocks version changes while a child is live.
- [x] 4.6 [Owner: Launcher] Terminate the managed POSIX DSH process group (or the direct Windows child) on explicit stop and Electron shutdown, and wait for root-child exit before a version mutation. Dependencies: 4.4. Evidence: `process-tree.test.ts` verifies POSIX process-group and Windows direct-child signal targets; `LauncherHarnessService` spawns a detached POSIX group and waits for exit; `main.ts` routes normal quit and `SIGINT`/`SIGTERM` through `shutdown()`.
- [x] 4.5 [Owner: Integration] Connect the running child to the renderer through typed preload and `dsh-app://` without an alternate transport. Dependencies: 1.2, 4.3. Capability: `desktop-renderer-authority`. Evidence: `RuntimeTabsPanel.vue` renders only `launch.url` from main, opens no page while stopped, withdraws pages when the runtime stops, and contains no hardcoded port; `tools/visual-smoke.mjs` asserts `runtime.no-hardcoded-url`.

## 5. Product experience

- [ ] 5.1 [Owner: Launcher] Build default first-run workspace creation with explicit error, cancellation, recovery, and later Settings relocation states. Dependencies: 2.4. Partially delivered: `ManagedWorkspacesPanel.vue` with `ManagedWorkspacesPanel.test.ts` covers bridge-unavailable, cancellation, and creation. Remaining: Settings relocation refusal has no UI or test.
- [ ] 5.2 [Owner: Launcher] Build clone, revision-selection, and worktree-retention flows that show remote, observed ref, exact SHA, and blocked conditions. Dependencies: 3.4. Partially delivered: `ManagedInstallationsPanel.test.ts` covers clone, revision-kind selection, switch, and rejection paths. Remaining: rewritten-ref handling is untested in the UI.
- [ ] 5.3 [Owner: Launcher] Show DSH profile plugins and Agent presets as native DSH-owned locations, separate from Launcher Settings. Dependencies: 3.5. Partially delivered: `VersionManagementPanel.vue` lists the read-only `profiles/web` dependencies and offers no Launcher mutation control. Remaining: the view does not name `$DSH_HOME/profiles/web` or `$DSH_HOME/.agent-presets` explicitly.
- [x] 5.4 [Owner: Launcher] Build run, readiness, diagnostics, stop, and recovery views for the child lifecycle. Dependencies: 4.4. Evidence: `LaunchPanel.vue`, `ControllerPanel.vue`, and `RuntimeTabsPanel.vue` render stopped, starting, running, and failed states; the console view streams the bounded stdout/stderr the service retains. `runtime-context-menu.test.ts` proves an attached Run guest exposes only guest-scoped navigation and editing actions, without developer tools or external navigation.
- [ ] 5.4.1 [Owner: Launcher] Add explicit fixed-step Run-page zoom, strict Settings-root persistence, full-bleed guest rendering, safe copyable rendering diagnostics, display-change re-observation, and restricted guest zoom accelerators. Dependencies: 2.5, 4.5, 5.4. Completion evidence pending: focused persistence, IPC-admission, guest-input, renderer-state, and Run-layout tests plus a same-URL, same-size, same-zoom Electron/Chrome comparison. Do not record a validation command here until it has run successfully.
- [ ] 5.5 [Owner: Launcher] Complete locale ownership, keyboard navigation, focus, contrast, long-path/ref handling, and accessibility states. Dependencies: 5.1, 5.2, 5.3, 5.4. Partially delivered: both locale catalogs are total over `MessageKey` (`i18n.test.ts`), and `ThemedListbox.vue` replaces every native select with a full ARIA listbox contract covered by 8 keyboard/state tests. Remaining: no contrast measurement or long-path/long-ref overflow evidence.
- [ ] 5.5.1 [Owner: Launcher] Verify that the Run zoom controls expose names, value, disabled endpoints, focus, and `Cmd`/`Ctrl` keyboard behavior; verify that 100-percent page zoom stays unchanged across supported display-scale changes and that the guest canvas uses no CSS rendering effects. Dependencies: 5.4.1. Completion evidence pending: accessibility assertions, reduced-motion coverage where applicable, macOS scale-transition evidence, and Windows 100/125/150/200-percent scaling evidence. Do not claim platform evidence that has not been collected.
- [x] 5.6 [Owner: Release] Publish bilingual README and usage guides, reproducible packaged-app screenshots, and Launch-page product orientation. Dependencies: 4.5. Evidence: `README.md`, `README.zh-CN.md`, `docs/usage.en.md`, and `docs/usage.zh-CN.md` describe GitHub Releases, Actions build evidence, native `.dsh` ownership, the first-run workflow, version/extension controls, and unsigned manual installation; packaged smoke writes the committed gallery under `docs/assets/screenshots`; Launch displays its compiled Launcher version, selected-core semantics, and the two fixed repositories; `external-links.test.ts` rejects renderer-supplied URLs.
- [x] 5.7 [Owner: Release] Rename the visible product, packaged artifact names, documentation, and fixed source URL to DSHKer Launcher while retaining `dsh-launcher` persistent identifiers. Dependencies: 5.6. Evidence: `npm run format:check`, `npm run type-check`, 51 focused tests, `npm run architecture:check`, `npm run visual:smoke`, `npm run package`, `npm run release:verify`, and `openspec validate add-managed-harness-desktop-shell --strict` pass; the local macOS artifact is `release/mac-arm64/DSHKer Launcher.app`.
- [ ] 5.8 [Owner: Launcher] Make root `package.json` the only Launcher version source and implement the Electron-main fixed-repository stable-release state machine, exact platform/architecture asset selection, typed preload admission, stale-download withdrawal, and system-browser handoff. Dependencies: 2.1, 5.7. Verify with focused version-source, semantic-version, GitHub-response, missing/duplicate-asset, sender-admission, and retained-download tests plus `npm run type-check` and `npm run architecture:check`; leave this task open until root records successful command output.
- [ ] 5.9 [Owner: Launcher] Add the Launcher Settings update section, explicit retry and download states, and one non-blocking startup notice only for a higher stable version, with no startup failure dialog or silent installation path. Dependencies: 5.8. Verify with renderer tests covering idle, checking, up-to-date, update-available, failed, startup timing, retry, exact installer copy, and accessibility; leave this task open until root records successful command output.

## 6. Verification and release readiness

- [x] 6.1 [Owner: QA] Run focused unit and integration suites for roots, persistence, executables, Git, worktrees, generations, readiness parsing, and recovery. Dependencies: 2.5, 3.5, 4.4. Evidence: `npm test -- --run` → 40 files, 173 tests passing; `npm run type-check`, `npm run architecture:check`, `npm run format:check`, `npm run visual:smoke`, `npm run service:smoke`, and `npm run seed:verify` pass; `node tools/validate-desktop-app.mjs --app apps/dsh-launcher --json` reports `ok: true`.
- [x] 6.2 [Owner: QA] Run a real Launcher-to-Harness smoke test using a selected managed worktree and stable native Harness home. Dependencies: 1.3, 4.5. Evidence: on 2026-09-03, the running DSHKer Electron app started the selected `db6bdc3576c2d4e7c965e8e3ed0c2a731eed87f5` checkout through typed IPC, observed its own `dsh web` loopback URL announcement, then stopped the managed child. The inherited native web profile was not rewritten by the Launcher.
- [ ] 6.3 [Owner: Release] Produce and verify packaged macOS and Windows builds, including declared signing state and cross-platform containment/lifecycle checks. Dependencies: 5.5, 6.1, 6.2. Partially delivered: `npm run release:readiness` passes end to end on macOS arm64 (12 stages, evidence in `.run/release-readiness/latest.json`). The packaged app now carries smoke instrumentation (`electron/main/smoke.ts`), so release evidence proves the real artifact mounts its shell, reaches all six routes, exposes the preload bridge, paints a non-blank multi-colour first frame, and logs no renderer errors. The workflow definition builds macOS arm64/x64 and Windows x64/arm64 artifacts for `main`, exact stable tags, and manual dispatches, and reserves public Release publication for an exact tag after all four native package smokes pass. `release/release-manifest.json` records `signingStatus: unsigned-local`; current publication therefore requires an explicit unsigned/manual-install warning and cannot enable in-application installation. Remaining: v0.1.11 through v0.1.17 built at least some targets but did not complete all four native smoke gates; v0.1.18 must provide four-platform publication evidence. Signing and macOS notarization remain prerequisites for any future automatic installation flow.
- [x] 6.3.1 [Owner: Release] Require a stable tag equal to `v${package.json.version}`, retain `main` and manual-dispatch installers as Actions artifacts, and publish a public latest GitHub Release only after both exact platform artifacts pass manifest and checksum validation. The publishing job SHALL combine installer checksums, retain platform-named manifests, use minimal `contents: write`, and fail instead of overwriting an existing Release. Dependencies: 5.8, 6.3. Verify workflow YAML parsing, workflow policy/static tests, a tag-mismatch rejection, manual-dispatch publication exclusion, release-asset assembly fixtures, and a successful tagged Actions run before checking this task. Evidence: tagged run 33858028371 completed successfully and published `v0.1.10` with five exact assets.
- [x] 6.3.2 [Owner: Release] Keep the Windows packaged smoke gate bounded with an explicit startup budget, keep the smoke window compositor-visible across resize checks, and preserve timeout/error evidence in the uploaded diagnostics. Dependencies: 6.3. Verify the Windows tagged run reaches a successful packaged smoke and the publishing job creates the exact public Release assets before checking this task. Evidence: v0.1.10 Windows x64 packaged smoke passed in run 33858028371 after keeping the smoke window at `(0, 0)`.
- [ ] 6.4 [Owner: Integration] Run strict OpenSpec validation and archive only after every task has matching evidence. Dependencies: 6.3, 6.3.1. Strict validation passes on 2026-09-03. Remaining: tasks 2.4, 3.5, 5.1, 5.2, 5.3, 5.5, 5.8, 5.9, 6.3, and 6.3.1 remain open.
